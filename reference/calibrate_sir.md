# Calibrate SIR Parameters (one-step)

Calibrate SIR Parameters (one-step)

## Usage

``` r
calibrate_sir(
  daily_cases,
  population_size,
  recovery_rate,
  model_dir = NULL,
  auto_init = TRUE
)
```
